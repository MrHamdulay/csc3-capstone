# Assignment 8: Question 4 
# Gadziraiushe Bangure: BNGGAD001
# May 9, 2014

# ----------------------------------------------------------------------
# a program that uses recursive functions to find all palindromic primes between two integers supplied as input (start and end points are included).a program that uses recursive functions to find all palindromic primes between two integers supplied as input (start and end points are included).
# ----------------------------------------------------------------------

import sys
sys.setrecursionlimit (30000)

N = eval(input("Enter the starting point N: \n"))
M = eval(input("Enter the ending point M: \n"))

def numbers_list (N,M):
    if N == M:
        return 
    else:
        return 
    



















print("The palindromic primes are: ")